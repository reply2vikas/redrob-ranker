numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0
pandas>=2.0,<3.0
pyyaml>=6.0
# Optional quality upgrade (semantic embeddings). Not needed for the
# self-contained TF-IDF+LSA ranker; only used by precompute_embeddings.py.
# sentence-transformers>=2.2
